All of the code for this assignment is saved in the zip file.

To run for stata code, each question and subpart has a .do file. The do file references data in the /data folder. 
The output is saved in the /figures folder (has both tables and graphs)

To run the R script for q2, run "Assig6_Q2.R" in the /R folder. 